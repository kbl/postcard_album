%h1 Edytuj widokówkę

= render :partial => 'form', :locals => { :button_label => t(:v_edit), :editable => false }

#bottom-navigation
  = link_to 'Pokaż edytowaną widokówkę', @postcard |
  = link_to 'Lista widokówek', postcards_path
